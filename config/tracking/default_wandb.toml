name = "default_wandb"
mode = "online"
project = "nvidia-nemotron-reasoning"
entity = "alexproger23"
tags = ["bootstrap", "v1", "wandb"]
output_root = "out/runs"
parquet_root = "out/analytics/parquet"
duckdb_path = "out/analytics/experiments.duckdb"
log_interval_steps = 10
save_resolved_config = true
